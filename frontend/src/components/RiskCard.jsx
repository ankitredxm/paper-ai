import { Box, Chip, Paper, Typography } from '@mui/material'; import { WarningAmberOutlined } from '@mui/icons-material'; import { riskColor } from '../utils/formatters';
export default function RiskCard({ risk, predicted, target }) { const deviation = ((predicted - target) / target) * 100; const color = riskColor(risk); return <Paper sx={{ p: 2.5, height: '100%' }}><Box display="flex" justifyContent="space-between"><Typography variant="subtitle2" color="text.secondary">PROCESS RISK</Typography><WarningAmberOutlined color={color} /></Box><Chip label={risk} color={color} sx={{ mt: 2, fontWeight: 700, fontSize: 16, height: 34 }} /><Typography mt={2} variant="body2" color="text.secondary">Deviation from target <Typography component="span" color="text.primary" fontWeight={700}>{deviation >= 0 ? '+' : ''}{deviation.toFixed(2)}%</Typography></Typography><Typography variant="caption" color="text.secondary">Target: {target} GSM</Typography></Paper>; }
