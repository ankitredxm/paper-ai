import { Alert, Box, Paper, Typography } from '@mui/material'; import { TipsAndUpdatesOutlined } from '@mui/icons-material';
export default function RecommendationCard({ recommendations }) { return <Paper sx={{ p: 2.5, height: '100%' }}><Box display="flex" gap={1} alignItems="center" mb={1.5}><TipsAndUpdatesOutlined color="primary" /><Typography variant="subtitle2" color="text.secondary">RECOMMENDED ACTIONS</Typography></Box>{recommendations.map((recommendation, index) => <Alert key={index} severity="info" icon={false} sx={{ mb: 1, py: .25, '& .MuiAlert-message': { fontSize: 13, fontWeight: 600 } }}>{recommendation}</Alert>)}</Paper>; }
