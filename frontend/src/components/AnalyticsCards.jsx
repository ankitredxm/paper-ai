import { Box, CircularProgress, Paper, Stack, Typography } from '@mui/material';
const metric = [{ label: 'Prediction accuracy', value: '96.8%', note: '+0.4% this week' }, { label: 'Daily production', value: '1,842 t', note: '86% of plan' }, { label: "Today's predictions", value: '128', note: '6 require attention' }];
export default function AnalyticsCards() { return <Stack direction={{ xs: 'column', md: 'row' }} spacing={2}>{metric.map((item, index) => <Paper key={item.label} sx={{ p: 2.25, flex: 1, display: 'flex', alignItems: 'center', gap: 2 }}><Box position="relative" display="inline-flex"><CircularProgress variant="determinate" value={index === 0 ? 97 : index === 1 ? 86 : 72} size={47} color={index === 0 ? 'success' : 'primary'} /><Typography variant="caption" sx={{ position: 'absolute', inset: 0, display: 'grid', placeItems: 'center', fontWeight: 700 }}>{index === 0 ? '97' : index === 1 ? '86' : '72'}</Typography></Box><Box><Typography variant="body2" color="text.secondary">{item.label}</Typography><Typography variant="h6">{item.value}</Typography><Typography variant="caption" color="success.main">{item.note}</Typography></Box></Paper>)}</Stack>; }
