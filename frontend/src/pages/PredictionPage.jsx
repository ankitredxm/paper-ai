import { Box, Grid, Typography } from '@mui/material'; import SensorForm from '../components/SensorForm'; import PredictionCard from '../components/PredictionCard'; import RiskCard from '../components/RiskCard'; import RecommendationCard from '../components/RecommendationCard'; import { usePrediction } from '../context/PredictionContext';
export default function PredictionPage() { const { result, lastInput } = usePrediction(); return <Box><Typography variant="h4" mb={.5}>Prediction workspace</Typography><Typography color="text.secondary" mb={3}>Submit current sensor values to generate an operational recommendation.</Typography><Grid container spacing={2.25}><Grid size={{ xs: 12, md: 4 }}><PredictionCard value={result.predicted_basis_weight}/></Grid><Grid size={{ xs: 12, md: 3 }}><RiskCard risk={result.risk} predicted={result.predicted_basis_weight} target={lastInput.target_basis_weight}/></Grid><Grid size={{ xs: 12, md: 5 }}><RecommendationCard recommendations={result.recommendations}/></Grid><Grid size={12}><SensorForm/></Grid></Grid></Box>; }
