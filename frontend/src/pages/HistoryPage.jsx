import { Box, Typography } from '@mui/material'; import HistoryTable from '../components/HistoryTable'; import Loader from '../components/Loader'; import { useHistory } from '../hooks/useHistory';
export default function HistoryPage() { const { data, loading, error } = useHistory(); return <Box><Typography variant="h4">History</Typography><Typography color="text.secondary" mb={3}>Review the latest 50 process predictions.</Typography>{loading ? <Loader/> : <><HistoryTable rows={data}/>{error && <Typography color="error" variant="caption" mt={1} display="block">{error}</Typography>}</>}</Box>; }
